package org.nktnet.middor.config

import org.nktnet.middor.R

enum class MirrorModeOption(val labelResId: Int) {
    OVERLAY(R.string.mirror_mode_overlay),
    ACTIVITY(R.string.mirror_mode_activity),
}
